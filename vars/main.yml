---
_tower_setup_venv_map:
  - pips: "ansible-tower-cli"
  - venv: "/var/lib/awx/venv/ansible-cloud-release"
    pips: "{{ tower_setup_common_pips + tower_setup_cloud_pips }}"
  - venv: "/var/lib/awx/venv/ansible-host-release"
    pips: "{{ tower_setup_common_pips + tower_setup_host_pips }}"
  - venv: "/var/lib/awx/venv/ansible-network-release"
    pips: "{{ tower_setup_common_pips + tower_setup_network_pips }}"
